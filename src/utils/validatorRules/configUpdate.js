const configUpdateRules = {
    runTimeConfigValue: {
        presence: {
            allowEmpty: false,
            message: "^pages/user-management:runtime_configuration.config_value_required_validation_message"
        }
    }
}

export default configUpdateRules
